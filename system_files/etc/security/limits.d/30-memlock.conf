# PROVENANCE: Bazzite values, verbatim — desktop/shared/etc/security/limits.d/
#   memlock.conf. The 30- prefix is ours.
#
# ~2 GiB. Anti-cheat runtimes under Proton and a few engines lock large buffers;
# the 8 MiB default makes them fail in ways that look like game bugs.
#
# The prefix matters: limits.d is read in lexicographic order and the base ships
# 25-pw-rlimits.conf (@pipewire memlock 4194304). PipeWire keeps its larger
# allowance regardless, since pam_limits ranks a group entry above `*`.
* hard memlock 2147484
* soft memlock 2147484
