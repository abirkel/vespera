# PROVENANCE: Bazzite, verbatim — desktop/shared/etc/geoclue/conf.d/99-beacondb.conf
#
# Mozilla Location Service shut down, leaving wifi geolocation broken on a stock
# system. BeaconDB is the community replacement.
[wifi]
enable=true
url=https://api.beacondb.net/v1/geolocate
