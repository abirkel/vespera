# PROVENANCE: Bazzite, verbatim — desktop/shared/etc/systemd/zram-generator.conf
#
# Compressed RAM swap. Replaces zram-generator-defaults (min(ram, 8192)) outright:
# a file in /etc wins over the one in /usr/lib.
[zram0]
compression-algorithm=zstd
zram-size = min(ram / 2, 16384)
