# PROVENANCE: Bazzite, verbatim — desktop/shared/usr/lib/sysctl.d/60-kernel.conf
#
# Split-lock detection throttles a process that issues an atomic op across a
# cache-line boundary. Several games and anti-cheat runtimes do it in hot loops and
# stall badly.
kernel.split_lock_mitigate=0
