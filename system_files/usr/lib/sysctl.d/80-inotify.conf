# PROVENANCE: Bazzite, verbatim — desktop/shared/usr/lib/sysctl.d/80-inotify.conf
#
# Steam libraries, file indexers and IDEs exhaust the Fedora default and then
# silently stop noticing file changes.
fs.inotify.max_user_instances=8192
fs.inotify.max_user_watches=524288
