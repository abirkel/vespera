# PROVENANCE: Bazzite, verbatim — desktop/shared/usr/lib/modules-load.d/
#   wine-ntsync.conf
#
# ntsync is Wine/Proton's in-kernel implementation of Windows sync primitives,
# replacing esync/fsync. A misc chardev with no modalias, so nothing autoloads it.
# No udev rule is needed: the upstream driver sets mode 0666 on the device itself.
ntsync
